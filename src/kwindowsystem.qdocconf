include($KDE_DOCS/global/qt-module-defaults.qdocconf)

project = KWindowSystem
description = Access to the windowing system.

documentationinheaders = true

headerdirs += .
sourcedirs += .

outputformats = HTML

depends += qtcore qtquick qtquickcontrols qtgui
